# Plan: Accesibilidad de componentes compartidos, Ajustes y copy (P5 + resto)

> **Para Codex:** Lee la spec de referencia en `docs/design/auditoria-ronda-3.md` (secciones P5 e P6)
> antes de empezar. **Crea la rama `fix/shared-accessibility-and-copy` desde `develop`** (ya tiene
> `develop` al dia, incluye P0-P4 fusionados). Commits separados por tarea, conventional commits,
> **sin Co-Authored-By**. Verifica local (`test` + `build`) antes de cada commit; NO lances el
> emulador. No commits de WIP.
>
> **Importante — no tocar estos ficheros**, los esta editando otra rama en paralelo
> (`refactor/home-stats-visual-locale`) sobre el mismo `develop`:
> `HomeScreen.kt`, `StatsScreen.kt`, `WorkoutScreen.kt` (excepto la linea exacta señalada en la
> Tarea 3, que es la unica excepcion), `LineChart.kt`, `Skeletons.kt`.

**Rama:** `fix/shared-accessibility-and-copy` (nueva, desde `develop`)
**Entorno macOS:** `export JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home && sh gradlew test`

---

## Tarea 0: Preparar rama

- [ ] `git checkout develop && git pull`
- [ ] `git checkout -b fix/shared-accessibility-and-copy`

---

## Tarea 1 (P4-4 parcial): Cabecera de Ajustes unificada

**Archivo:** `feature/settings/SettingsScreen.kt`

- [ ] Sustituir el `TopAppBar` de Ajustes por `FitTrackScreenHeader` (mismo patron ya usado en
      Historial/Rutinas/Entrenar), manteniendo el boton de volver como `leading`
- [ ] `test` + `build` en verde
- [ ] Commit: `refactor: unify settings header with the shared screen header pattern`

---

## Tarea 2 (P4-10): Intro con paleta oscura y duracion no fija

**Archivo:** `feature/launch/LaunchIntroScreen.kt`

- [ ] La pantalla de intro usa una paleta duplicada, solo clara — leer `MaterialTheme.colorScheme`
      en vez de colores fijos, para que respete tema oscuro
- [ ] La duracion esta fijada a 1240ms — evaluar si conviene un valor configurable/mas corto, o
      al menos documentar por que se mantiene si se decide no tocarla
- [ ] `test` + `build` en verde
- [ ] Commit: `fix: make launch intro respect dark theme and document its fixed duration`

---

## Tarea 3 (P5-1): Roles de accesibilidad en clickables pelados

**Archivos:** `core/design/AppShell.kt` (drawer rows, ~lineas 356 y 418),
`core/design/components/ThemeModeSelector.kt` (~linea 88),
`core/design/components/SegmentedSelector.kt` (~linea 47),
`feature/workout/WorkoutScreen.kt` (~linea 1009, alternativa de ejercicio)

- [ ] Añadir `Role` y `onClickLabel` donde falte: el selector de tema deberia declarar
      `Role.RadioButton`, el segmentado `Role.Tab`, las filas del drawer `Role.Button` (o el que
      corresponda segun su semantica real)
- [ ] En `WorkoutScreen.kt`, tocar UNICAMENTE la linea del `.clickable` de la tarjeta de
      alternativa de ejercicio (~1009) para añadirle rol — no toques nada mas de ese fichero,
      hay otra rama trabajando el resto
- [ ] `test` + `build` en verde
- [ ] Commit: `fix: add semantic roles to plain clickables across shared components`

---

## Tarea 4 (P5-2 y P5-3): Objetivos tactiles minimos y contentDescription correcto

**Archivo:** `core/design/components/Stepper.kt`, `core/design/SetRow.kt`

- [ ] El stepper compacto usa 28dp, el badge de completar 30dp: mantener la caja visual pero
      expandir el area tactil al minimo recomendado (48dp) con `Modifier.minimumInteractiveComponentSize()`
      o un padding equivalente que no cambie el tamaño visible
- [ ] TalkBack lee el id de Room ("Bajar peso de la serie 4718") en el stepper de peso; el
      stepper de reps, unas lineas mas abajo, ya usa `setNumber` correctamente — copiar ese patron
      al de peso
- [ ] `test` + `build` en verde
- [ ] Commit: `fix: expand minimum touch targets and fix set number in accessibility labels`

---

## Tarea 5 (P6-2): Falta la ñ en "Anadir"

**Archivo:** `feature/routines/RoutinesScreen.kt` (4 apariciones, busca "Anadir")

- [ ] Corregir las 4 apariciones de "Anadir" a "Añadir" — no es un tema de tildes omitidas
      (el proyecto ya escribe sin tildes en varios sitios a proposito), es que falta la letra ñ,
      que en español no es opcional
- [ ] `test` + `build` en verde
- [ ] Commit: `fix: correct missing enye in Anadir across routines screen`

---

## Tarea 6 (P6-3 parcial): Plurales en el widget

**Archivo:** `FitTrackPlusWidget.kt`

- [ ] "Racha: 1 dias", "1 sesiones" — el widget tiene una logica de plural manual incorrecta;
      Home ya tiene la version correcta (duplicada ~250 lineas mas abajo en `HomeScreen.kt`, pero
      **no toques `HomeScreen.kt`**, esta en la otra rama) — replica la logica correcta solo en el
      widget, sin depender de un import cruzado que fuerce tocar `HomeScreen.kt`
- [ ] `test` + `build` en verde
- [ ] Commit: `fix: use correct singular/plural forms in widget streak and session labels`

---

## Verificacion final antes de avisar al dueño

- [ ] `sh gradlew test` en verde
- [ ] `sh gradlew build` en verde
- [ ] Confirmar que no se toco ningun fichero de la lista prohibida salvo la linea exacta de la Tarea 3
- [ ] Push y avisar, marcando cualquier tarea recortada de alcance

**Pendiente de pasada manual (la hace el dueño):**
1. TalkBack activado: revisar que el selector de tema, el segmentado y las filas del drawer se
   anuncian con el rol correcto.
2. Revisar visualmente el intro en tema oscuro.
3. Comprobar los objetivos tactiles del stepper compacto y el badge de completar con el dedo, no
   solo con el raton en el emulador.
4. Confirmar "Añadir" en las 4 pantallas de rutinas.

**Merge:** a `develop` cuando compile, pasen los tests, y la rama hermana
`refactor/home-stats-visual-locale` este tambien lista (para evitar conflictos de integracion
avisando entre las dos antes de mergear cualquiera).
